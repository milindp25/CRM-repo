# =============================================================================
# Admin Web App (Next.js) - Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# This frontend only needs public URL configs — no secrets needed.
# Authentication is handled by Tenant API, admin ops by Admin API.
# =============================================================================

# -----------------------------------------------------------------------------
# Public (exposed to browser)
# -----------------------------------------------------------------------------
# Tenant API — used for login (auth endpoints)
NEXT_PUBLIC_API_URL="http://localhost:4000/v1"

# Admin API — used for all admin operations
NEXT_PUBLIC_ADMIN_API_URL="http://localhost:4001/v1"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3001"
